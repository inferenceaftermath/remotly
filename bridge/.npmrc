# Installing the bridge's dependencies never runs their install scripts: none of them needs one, and a package that
# suddenly grows one is exactly what this stops. bridge/scripts/package.sh installs the release tarball the same way.
ignore-scripts=true
